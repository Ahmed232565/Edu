# EduMasr — Vue (CDN) App

- Pure Vue 3 via CDN (no build step).
- Light/Dark themes per your palette.
- Arabic RTL, responsive (mobile + desktop).
- Role-aware navigation for: Students, Parents, Ministry Supervisors.
- Mock data for schools, buses, store, jobs, learning, contact.
- LocalStorage auth (email/phone + password) with role selection.

## Run
Just open `index.html` in a browser, or host the folder on any static host (Vercel, Netlify, GitHub Pages).
